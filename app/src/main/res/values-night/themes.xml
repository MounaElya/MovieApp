<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Movies" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/grey</item>
        <item name="colorPrimaryVariant">@color/rouge</item>
        <item name="colorOnPrimary">@color/rouge</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/red</item>
    </style>
    <style name="Theme.Movies" parent="Base.Theme.Movies" />
</resources>